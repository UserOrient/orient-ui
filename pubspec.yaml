name: orient_ui
description: "Theming and components for Flutter. Works with Material, Cupertino, or on its own."
version: 0.5.0
homepage: https://userorient.com
repository: https://github.com/userorient/orient-ui

exclude:
  - templates/**

executables:
  orient_ui:

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0